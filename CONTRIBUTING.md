# Contributing to WPStow

感谢参与 WPStow。提交改动前请：

1. 从 `main` 创建独立分支。
2. 不要提交云存储凭据、站点日志或真实媒体文件。
3. 对所有 PHP 文件运行 `php -l`，对 `static/*.js` 运行 `node --check`。
4. 涉及存储驱动时，至少验证上传失败不会删除本地文件，并验证测试对象可以清理。
5. 在 Pull Request 中说明兼容的 WordPress、PHP 和存储服务版本。

提交代码即表示你同意按项目的 MIT 许可证发布该贡献。
